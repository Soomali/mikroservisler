apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
  labels:
    app: restaurant-service
spec:
  replicas: 1 # You can adjust the number of replicas as per your requirement
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      containers:
      - name: restaurant-service
        image: restaurant-service
        imagePullPolicy: Never
        ports:
        - containerPort: 3000 # Change the port as per your application
        env:
        - name: MONGODB_URL
          value: mongodb://mongodb-service:27017/restaurant
        - name: TYPE
          value: PROD
        - name: KAFKA_URLS
          value: message-broker:9092