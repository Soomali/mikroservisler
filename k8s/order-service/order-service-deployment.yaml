apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  replicas: 1 # You can adjust the number of replicas as per your requirement
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        image: order-service
        imagePullPolicy: Never
        ports:
        - containerPort: 3000 # Change the port as per your application
        env:
        - name: MONGODB_URL
          value: mongodb://mongodb-service:27017/order
        - name: TYPE
          value: PROD
        - name: KAFKA_URLS
          value: message-broker:9092